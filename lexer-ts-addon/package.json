{
  "name": "lexer-ts-addon",
  "version": "1.0.0",
  "description": "Makes it possible to call the lexer from typescript code.",
  "main": "./out/lexer_addon.js",
  "private": true,
  "keywords": [],
  "author": "Gergely",
  "license": "ISC",
  "dependencies": {
    "bindings":"^1.5.0",
    "node-addon-api": "^8.2.2"
  },
  "gypfile": true,
  "scripts": {
    "test-root": "tsc -b && node ./out/projectRoot.js",
    "clean-gyp": "node-gyp clean && node-gyp configure",
    "rebuild": "tsc -b && node-gyp rebuild",
    "test-main": "tsc -b && node out/main.js"
  },
  "devDependencies": {
    "@types/node": "^22.8.7"
  },
  "exports": {
    ".": {
      "import": "./out/lexer_addon.js",
      "require": "./out/lexer_addon.js"
    }
  }
}
